using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//class used to animate the current sprite towards another, play an animation and return back to its original position.

public class attackTarget : MonoBehaviour {

	public GameObject target;
	public string attackType;
	public float fightXPos;
	public float fightYPos;

	//need to remember to reference the script Lerp of this object. 

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
		
}
